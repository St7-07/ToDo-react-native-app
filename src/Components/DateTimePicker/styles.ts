import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 14,
    fontWeight: '600',
    marginBottom: 8,
  },
  fieldContainer: {
    marginTop: 3 * 8,
  },
  icon: {
    marginHorizontal: 16,
  },
  ios: {width: '200%', marginRight: 90},
  selector: {
    flexDirection: 'row',
    width: '100%',
    backgroundColor: '#e2e0df',
    borderRadius: 5,
    height: 40,
    paddingLeft: 8,
    textAlign: 'center',
    alignItems: 'center',
  },
});

export default styles;
